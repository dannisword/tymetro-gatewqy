import {
  mdiMenu,
  mdiClose,
  mdiAccount,
  mdiTextBoxOutline,
  mdiViewDashboard,
  mdiMonitor,
} from "@mdi/js";
export function IconHandle() {
  return { mdiMenu, mdiClose, mdiAccount, mdiTextBoxOutline, mdiViewDashboard, mdiMonitor };
}
